IF( QT4_FOUND AND QT_VERSION_MINOR GREATER 4 )
  SUBDIRS( GUI )
ENDIF( QT4_FOUND AND QT_VERSION_MINOR GREATER 4 )

SUBDIRS( IO )
SUBDIRS( ExternalCode )

IF( ITK_VERSION_MINOR GREATER 14 )
  SUBDIRS( Filters )
ENDIF( ITK_VERSION_MINOR GREATER 14 )
