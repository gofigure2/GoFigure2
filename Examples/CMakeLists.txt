IF( BUILD_EXAMPLES )
  ADD_SUBDIRECTORY( ExternalCode )
  ADD_SUBDIRECTORY( IO )
ENDIF( BUILD_EXAMPLES )

IF( BUILD_EXAMPLES OR BUILD_PLUGINS )
  ADD_SUBDIRECTORY( GUI )
ENDIF( BUILD_EXAMPLES OR BUILD_PLUGINS )
